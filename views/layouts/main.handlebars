<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>Document</title>
</head>

<body>
  <header>
    <img class="img-nav" src="/assets/img/cat.png" alt="">
    <nav>
      <ul>
        {{!-- {{#if session.id}} --}}
        <a href="/login">
          <li>Login</li>
        </a>

        <a href="/cadastro">
          <li>Cadastro</li>
        </a>
        {{!-- {{else}}
        <a href="/logout">
          <li>Sair</li>
        </a>
        {{/if}} --}}

      </ul>

    </nav>
  </header>

  {{#if messages.message}}
  <div class="message">
    {{messages.message}}
  </div>
  {{/if}}
  {{{body}}}
</body>

</html>